# إغلاق M02 — State Machine للتعلم والتكيف

**التاريخ:** 2026-08-28
**الفرع:** `recovery/ui-media-admin-overhaul`
**الحالة:** CLOSED — مع نقل دورة التقوية الكاملة إلى M03 كما هو مخطط.

## ما تم تثبيته

1. فصل Placement عن Learning Adaptation وعن Level Transition.
2. إضافة سياسة نقية مستقلة في `services/api/learning_state_machine.py`.
3. تثبيت حدود النشاط:
   - `>= 80` = PASS.
   - `70 <= score < 80` = GUIDED_RETRY.
   - `< 70` = REINFORCEMENT.
4. رفض القيم خارج 0..100 بدل قصها بصمت.
5. منع الانتقال قبل إكمال 10/10 من الأنشطة الأساسية للمستوى.
6. منع الانتقال عند وجود تقوية معلقة أو نقص تغطية مهارية أو مهارة حرجة غير معالجة.
7. تثبيت الانتقال مستوى واحد فقط:
   - L1 → L2.
   - L2 → L3.
   - L3 → Journey Complete، بلا مستوى رابع.
8. الحفاظ على Core Session مستقلة لكل مستوى وعدم إعادة تسمية التاريخ القديم.
9. الحفاظ على المحاولات المحايدة (فجوات الوسائط والصوت غير المحسوم والتخطي المؤقت) خارج Evidence الأكاديمي.
10. الحفاظ على 50/30/20 كمؤشر Mastery/Trend فقط، وعدم استخدامه لتجاوز بقية أنشطة المستوى.

## Guided Retry

الـruntime الحالي يملك أصلًا إعادة موجهة على مستوى الجولة: أول إجابة خاطئة تبقي الجولة مفتوحة وتعرض hint/retry قبل إغلاق الجولة. M02 يثبت المصطلح والحدود الأكاديمية الموحدة. أي توسعة لدورة إعادة الجزء الضعيف بعد التقوية نفسها تُنفذ في M03 ضمن دورة:

`Weakness → Reinforcement → Return to core → Verification → Continue`

## Automatic Demotion

لم يتم حذفه في M02. ما يزال قرار إلغائه من الرحلة العادية **مقترحًا يحتاج اعتمادًا نهائيًا** حسب `ADAPTATION_REINFORCEMENT_REDESIGN_2026-08-28_AR.md`. لا يجوز لمنفذ لاحق حذف هذا السلوك دون ADR واعتماد صريح.

## الاختبارات

تمت إضافة `services/api/test_learning_state_machine.py` لتغطية:
- 80.
- 79.9999.
- 70.
- 69.9999.
- منع الترقية عند 9/10.
- حجب الانتقال بسبب التقوية/التغطية/المهارة الحرجة.
- L1→L2 وL2→L3.
- نهاية L3 بلا L4.

Quality Gate #230 على commit `917c3000de106db0b0a73fb6feaba7c6f2bc56ab`:
- Backend: SUCCESS.
- Frontend: SUCCESS.
- Integration/Playwright: SUCCESS.

## المرحلة التالية

**M03 — Reinforcement Mapping + 18 إضافات تقوية + دورة الرجوع والتحقق.**

الأولوية داخل M03:
1. عدم تعديل الكتالوج الأصلي 105 باعتباره Source Baseline.
2. إدخال التقويات الجديدة كامتداد Content Versioned واضح.
3. بناء Skill → Skill Family → Approved Reinforcement Candidates.
4. إزالة اعتماد Exact `skill_id` كآلية وحيدة.
5. لا random fallback.
6. بعد التقوية يجب العودة إلى النشاط/الجولة التي كشفت الضعف للتحقق قبل الاستمرار.
7. منع loop لا نهائي وإحالة الحالة للمشرف عند استمرار الضعف.
