# إغلاق M00 — استعادة HEAD أخضر

**التاريخ:** 2026-08-28

**الفرع:** `recovery/ui-media-admin-overhaul`

**الحالة:** `ACCEPTED / CLOSED`

## الهدف

إعادة الفرع الحالي إلى Quality Gate أخضر دون Reset ودون حذف أو التراجع عن إصلاحات التكيف والواجهات والوسائط الأخيرة.

## نقطة البداية

كان آخر HEAD برمجي قبل صيانة اليوم:

`0f46e7e9421e7617f72fd03de3199df955084cd0`

GitHub Actions #206 / Run `33139416128`:

- Frontend: SUCCESS.
- Backend: SUCCESS.
- Integration/Playwright: FAILURE.

السبب لم يكن فشل البنية التحتية؛ الاختبار المتكامل كان يحمل افتراضًا قديمًا بأن شاشة المشرف يجب أن تعرض `10 من 10` مباشرة بعد مسار التعلم، بينما الكود الحديث يفتح Core Session جديدة عند الانتقال إلى المستوى التالي، ولذلك يصبح تقدم المستوى الحالي منفصلًا عن المستوى السابق.

## الإصلاح الأول

Commit:

`98bc121108a343697aa60e45bc853689fbeedc08`

الرسالة:

`test: align E2E with multi-level learning journey`

تم تعديل اختبار Playwright ليقرأ الحالة الحقيقية من API الطالب ويثبت:

- `current_level` الحالي.
- `core_completed_items/core_total_items` للمستوى الحالي.
- أن الانتقال بين المستويات لا يعيد تسمية تاريخ المستوى السابق.
- أن `posttest_eligible` يبقى False ما دام الطالب قبل نهاية مسار المستوى الثالث.

GitHub Actions #220 / Run `33140701035` وصل إلى المسار الصحيح وظيفيًا، لكنه فشل بسبب Playwright strict selector فقط: النص `المستوى 1` كان يطابق فقرة وخيار select ونصًا آخر.

## الإصلاح الثاني

Commit:

`beec2d38a32c499c483372f4d329b26d929fb4d8`

الرسالة:

`test: use unambiguous current-level locator`

تم تضييق locator إلى فقرة المستوى المطابقة تمامًا دون تخفيض أي توقع وظيفي.

## الدليل النهائي

GitHub Actions #221 / Run ID:

`33141084000`

HEAD:

`beec2d38a32c499c483372f4d329b26d929fb4d8`

النتيجة النهائية:

- Frontend: SUCCESS.
- Backend: SUCCESS.
- Integration: SUCCESS.
- Playwright E2E: SUCCESS.

وشملت البوابة:

- TypeScript + ESLint + unit tests + Next.js production build.
- PostgreSQL 16.
- Alembic migrations ومراجعة drift.
- seed للكتالوج المعتمد 105 عناصر.
- Redis.
- MinIO pinned/checksummed وتشغيل bucket حقيقي داخل CI.
- FastAPI + Next.js.
- Chromium Playwright للمسار من الصفحة العامة إلى المشرف والطالب والقبلي والتسجيل والمراجعة والتعلم التكيفي والتقوية والتقارير.

## ملاحظات أثناء الفحص

ظهر في سجل PostgreSQL تعارض Unique متوقع على `uq_reward_event_student_key` أثناء محاولة إنشاء reward مكرر. المسار الحالي يعالجه داخل savepoint ولم يسبب فشل backend أو integration. يبقى هذا دليلًا على أن اختبار race/idempotency يُمارس فعليًا وليس سبب فشل حالي.

## القرار

M00 مغلق. لا يوجد مبرر لعمل Reset أو حذف commits الحديثة.

**المهمة التالية:** `M01 — Placement Scoring & Gates` وفق `FULL_MAINTENANCE_PLAN_2026-08-28_AR.md`، مع الالتزام بدرجات 20/40/40 وبوابة الاستعداد 12/20 وعدم اختراع حدود رقمية لبوابات L2/L3 غير المعتمدة في المصدر.