# M04 — تقدم إعادة بناء تجربة الطالب

**التاريخ:** 2026-08-28  
**الفرع:** `recovery/ui-media-admin-overhaul`  
**الحالة:** IN PROGRESS — لا تُعتبر مغلقة قبل تحقق CI وPlaywright والمراجعة البصرية متعددة المقاسات.

## الهدف

تحويل تجربة الطالب من صفحات/Card تقليدية إلى **Learning Stage** عربية RTL مرنة ومناسبة لطالب الصف الثالث، مع إبقاء منطق التقييم والأنشطة والتقوية والصوت كما هو دون Regression.

## ما أُنجز قبل هذا التقرير

1. أصبح النشاط يعرف هل هو `core_activity` أم `reinforcement_activity` ويعرض لغة طفولية مختلفة للتقوية.
2. أصبحت نهاية L1 وL2 تقود إلى المستوى الأعلى، بينما L3 وحده يقود إلى أهلية الاختبار البعدي بعد فتح المشرف.
3. أضيفت اختبارات Frontend لنصوص التقوية ومسار الانتقال للأعلى.
4. أُصلح تعارض الاختبار القديم الخاص بالاختبار البعدي دون إضعاف بوابة رحلة L1→L2→L3:
   - الاختبار القديم صار يصرّح أن سيناريوه يبدأ من L3 بدل افتراض أن إكمال أي مستوى يفتح البعدي.
   - Run #274 / ID `33194902657` أصبح أخضر بالكامل: Frontend ✅ Backend ✅ Integration/Playwright ✅.

## التغيير البصري الحالي

Commit: `548c4e8dc4d07939b33acb155a50dbda744244f5`

تم تحويل `apps/web/src/app/student/activity/activity.module.css` إلى أساس Learning Stage أكثر نضجًا:

- استخدام `100dvh` بدل صفحة قصيرة داخل فراغ كبير.
- توسيع مساحة العمل إلى حد أقصى يقارب 1280px بدل Card ضيقة 900px.
- تخصيص مساحة جانبية واضحة للشخصية التعليمية على الشاشات الكبيرة.
- رفع حجم الشخصية من ~82px إلى ~155–215px في الجلسة التعليمية.
- وضع رسالة الشخصية في Bubble مستقلة بجانبها بدل ظهورها كملحوظة صغيرة أسفل النشاط.
- إعطاء منطقة التفاعل بقية مساحة الشاشة والتركيز البصري.
- تحسين خيارات النص والصور والتسلسل والتسجيل وحالات Focus.
- إزالة الاعتماد على Gradient كبير في أزرار التفاعل الرئيسية لصالح ألوان الهوية المسطحة والواضحة.
- تحسين Responsive للشاشات حتى 960px و720px و480px.
- على الهاتف تعود الشخصية إلى جزء من التدفق بدل Overlay جانبي.
- دعم الشاشات القصيرة Desktop/Landscape عبر `max-height` media rule.
- احترام `prefers-reduced-motion`.

## ما لم يُنجز بعد في M04

لا يجوز اعتبار M04 مكتملة بعد. المتبقي:

1. فحص Run #275 الخاص بالـLearning Stage وإصلاح أي Regression.
2. مراجعة Playwright screenshots بصريًا وعدم الاكتفاء بأن الاختبار أخضر.
3. توحيد نفس Shell مع الاختبار القبلي والبعدي حتى لا يبدو كل جزء من المنصة كنظام مختلف.
4. تحسين زر الخروج إلى مفهوم «حفظ والخروج» حيث ينطبق مع رسالة تأكيد أن التقدم محفوظ.
5. ترقية حالات التسجيل الصوتي إلى حالات UX رسمية: جاهز → يسجل → مسجل → استماع → رفع → نجاح/انتظار، مع مسارات الخطأ.
6. اختبار 360×800، 390×844، 768×1024، 1024×768، 1440×900 دون Horizontal Overflow.
7. توحيد Loading / Empty / Error / Waiting states.
8. مراجعة حجم الشخصية ومكانها لكل نوع تفاعل، خصوصًا النصوص الطويلة والصور والتسلسل.
9. Screenshot review نهائي قبل إغلاق M04.

## ثوابت لا تُكسر أثناء M04

- لا تعديل للمحتوى الأصلي الـ105.
- لا اختراع صور/أصوات بدل الملفات المعتمدة.
- فجوتا الصوت الثابت المعروفتان تظلان: `موز` و`سَا`.
- `HIMMA_TEMP_AUDIO_SKIP` يظل محايدًا أكاديميًا ولا يخلق صوتًا أو درجة أو تقوية.
- مسار المستويات: placement يحدد البداية، ثم صعود فقط إلى L3 وفق العقد الحالي، بينما قرار auto-demotion القديم ما زال قرارًا منفصلًا غير محسوم ولا يُحذف صامتًا.
- الاختبار البعدي لا يفتح قبل اكتمال L3.
- ASR الحقيقي غير مكتمل ولا يُدّعى اكتماله.

## نقطة الاستئناف

1. افحص Run #275 (`33195302100`).
2. إذا أخضر، نزّل Playwright artifact وراجع لقطات النشاط بصريًا.
3. إذا ظهر خلل، أصلحه على نفس الفرع ولا تعمل Reset للإصلاحات الأكاديمية.
4. بعدها أكمل توحيد Student Task Shell للاختبارات والأنشطة والتقوية.
