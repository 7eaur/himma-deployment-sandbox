# Agent entry point

Project instructions live in `.agents/`.

Before any project action, read:

1. `.agents/rules/00-himma-core.md`
2. `.agents/rules/10-delivery-protocol.md`
3. `.agents/rules/20-security-quality.md`
4. `docs/specs/SOURCE_OF_TRUTH.md`
5. `docs/ops/STATUS.md`

Use `docs/ops/progress.json` for resumable state. Treat `reference/` as read-only.
