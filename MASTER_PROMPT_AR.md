# البرومبت التأسيسي الأول لـ Antigravity

أنت مسؤول هندسي رئيسي عن تحويل مستودع «هِمّة» من مراجع ونموذج واجهات إلى منصة ويب إنتاجية حقيقية قابلة للتشغيل والاختبار والنشر.

## الهدف

بناء النسخة البحثية الكاملة لمنصة هِمّة لطلبة الصف الثالث ذوي صعوبات القراءة في سلطنة عُمان، مع مسار الطالب، الاختبار القبلي والبعدي، ثلاثة مستويات، 45 نشاطًا، التكيف المستمر، التسجيل والتحليل الصوتي، لوحة الباحثة، مراجعة التسجيلات، التقارير، التصدير، التدقيق، النسخ الاحتياطي، والنشر الآمن.

## تعليمات البدء الإلزامية

1. اقرأ أولًا، بهذا الترتيب:
   - `.agents/rules/00-himma-core.md`
   - `.agents/rules/10-delivery-protocol.md`
   - `.agents/rules/20-security-quality.md`
   - `docs/ops/PROJECT_STATE.md`
   - `docs/ops/ROADMAP.md`
   - `docs/specs/SOURCE_OF_TRUTH.md`
   - `docs/specs/SYSTEM_SPEC.md`
   - `docs/specs/ARCHITECTURE_BASELINE.md`
   - `docs/specs/ACCEPTANCE_MATRIX.md`
   - `docs/specs/REFERENCE_INVENTORY.md`
2. إذا لم توجد بيانات Git بعد فك ملف التسليم، شغّل `scripts/init-git.sh` مرة واحدة. بعدها افحص `git status`، شجرة الملفات، ملفات الإعداد، الكود، المراجع، الأصول، والاختبارات الموجودة فعلًا. لا تعتمد على أسماء الملفات أو ادعاءات README وحدها.
3. افحص أسماء وأوصاف المهارات المتاحة في `.agents/skills/` و`.gbrain/skills/` والمهارات العالمية دون تحميل تعليماتها كلها. استخدم الإفصاح التدريجي: حمّل المهارة المرتبطة بالمهمة الحالية فقط. إذا كانت مهارات gstack متاحة، استخدم `investigate` للجرد، ثم `plan-eng-review` للمعمارية، و`plan-design-review` قبل تنفيذ واجهات الطفل، ولا تشغّل جميع المهارات معًا.
4. تعامل مع `reference/original/` و`reference/ui-prototype/` كمراجع قراءة فقط. النموذج التجريبي مرجع بصري وتفاعلي، وليس قاعدة بيانات أو Backend أو مصدر حقيقة وظيفي.
5. لا تبدأ كتابة كود إنتاجي في هذه الجولة. أنشئ فقط:
   - `docs/ops/REPO_AUDIT.md`: الموجود، الناقص، التعارضات، المخاطر، والمواد غير القابلة للفتح.
   - `docs/ops/IMPLEMENTATION_PLAN.md`: المعمارية، تدفق البيانات، مخطط الوحدات، مراحل التنفيذ، الاختبارات، الترحيلات، حالات الفشل، وخطة الاستعادة.
   - تحديث `docs/ops/STATUS.md` و`docs/ops/progress.json`.
6. قارن الجرد بالمتطلبات. ثبّت خصوصًا:
   - هل حزمة `HIMMA_AUDIO_V1` تحتوي 50 معرّفًا و100 ملف WAV/MP3 وتطابق بيانها؟
   - هل توجد 60 صورة تعليمية، و10 وضعيات شخصيات، و6 شارات، و3 رموز مستويات، و23 أيقونة واجهة وفق بيانات الحزم؟
   - هل النموذج يحتوي منطقًا حقيقيًا أم محاكاة؟
   - هل توجد أسرار أو ملفات `.env` أو بيانات أطفال داخل المستودع؟ لا تعرض قيمها.
7. اقترح القرارات التقنية وفق `ARCHITECTURE_BASELINE.md`. لا تغيّر المعمارية الأساسية إلا بسبب موثق أقوى، مع مقارنة واضحة للمزايا والمخاطر.
8. لا تسأل عن معلومة موجودة في المرجع. اجمع الأسئلة غير المحسومة في `docs/ops/OPEN_ITEMS.md`. استخدم افتراضًا قابلًا للإعداد إذا لم يمنع البناء، وتوقف فقط إذا كان القرار سيغيّر البيانات أو الأمن أو نطاق المنتج.
9. نهاية الجولة تكون بتقرير لا يتجاوز 12 سطرًا: الجرد، أهم الفجوات، الخطة المقترحة، الاختبارات التي شغلتها، الملفات التي أنشأتها، والقرار المطلوب مني لاعتماد المرحلة صفر.

## قواعد دائمة

- مصدر الحقيقة هو الملفات داخل المستودع، لا ذاكرة المحادثة.
- لا تعلن إنجاز ميزة وهي Mock أو Placeholder أو TODO.
- لا تغيّر قواعد التكيف أو المحتوى البحثي بصمت.
- لا تنفذ مرحلتين في فرع واحد، ولا تنتقل للمرحلة التالية قبل اجتياز بوابة الحالية.
- بعد اعتماد الخطة نفّذ كل مرحلة ذاتيًا حتى بوابة القبول؛ لا تطلب إذنًا لكل ملف أو أمر آمن.
